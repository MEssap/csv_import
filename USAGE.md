# 快速使用指南

## 编译项目

```bash
# 开发版本（快速编译）
cargo build

# 生产版本（优化性能）
cargo build --release
```

## 运行应用

```bash
# 开发版本
cargo run

# 生产版本（推荐）
cargo run --release

# 或直接运行二进制文件
./target/release/csv_import  # Linux/macOS
.\target\release\csv_import.exe  # Windows
```

## 使用步骤

### 1. 配置 Elasticsearch

启动应用后，在界面顶部配置区设置：

- **ES 地址**: 默认 `http://192.168.1.200:9200`，可根据实际情况修改
- **用户名**: 如果 ES 需要认证，填写用户名（可选）
- **密码**: 如果 ES 需要认证，填写密码（可选）
- **索引名称**: 输入目标索引名称，如 `my_data`（必填）
- **批次大小**: 默认 `1000`，可根据网络和 ES 性能调整
- **分隔符**: 默认"自动检测"，也可手动选择

### 2. 选择文件

两种方式：

**方式一：选择单个或多个文件**
- 点击"选择文件"按钮
- 在文件对话框中选择一个或多个 CSV 文件
- 支持 Ctrl/Cmd+点击 多选

**方式二：选择整个文件夹**
- 点击"选择文件夹"按钮
- 选择包含 CSV 文件的文件夹
- 程序会自动扫描该文件夹下所有 `.csv` 文件

### 3. 开始导入

- 点击"开始导入"按钮
- 观察进度条和日志输出
- 等待导入完成

### 4. 查看结果

导入完成后，在统计区查看：
- 成功导入的记录数
- 失败的记录数
- 跳过的记录数
- 总耗时

## 功能说明

### 自动分隔符检测

程序会自动分析 CSV 文件前 10 行，检测以下分隔符：
- 逗号 `,`
- 分号 `;`
- 制表符 `\t`
- 竖线 `|`

选择列数最一致的作为分隔符。

### 编码自适应

程序会按以下顺序尝试解码：
1. UTF-8（包括带 BOM 的文件）
2. GBK/GB18030
3. 强制 UTF-8（替换无效字符）

### 智能分块

如果 CSV 文件估算超过 2,147,483,647 行（ES 单索引上限），程序会自动分块：

例如，索引名为 `my_data`：
- 第一个分块：`my_data_01`
- 第二个分块：`my_data_02`
- 第三个分块：`my_data_03`
- ...

如果总行数未超过上限，则使用原始索引名 `my_data`。

### 错误处理

- **网络错误**：自动重试，最多 3 次，使用指数退避（1s → 2s → 4s）
- **格式错误**：跳过该行，记录到日志
- **超长记录**：单条记录 >100MB 时跳过，记录警告

## CSV 文件格式示例

```csv
姓名,年龄,城市,职业
张三,25,北京,工程师
李四,30,上海,设计师
王五,28,深圳,产品经理
```

导入到 Elasticsearch 后：

```json
{
  "姓名": "张三",
  "年龄": "25",
  "城市": "北京",
  "职业": "工程师"
}
```

## 性能优化建议

1. **使用 Release 版本**：性能提升 10-100 倍
   ```bash
   cargo build --release
   ./target/release/csv_import
   ```

2. **调整批次大小**：
   - 网络较慢：减小到 500
   - 网络较快：增大到 2000-5000
   - 默认 1000 适合大多数场景

3. **ES 配置优化**：
   - 增加 `bulk` 线程池大小
   - 调整 `refresh_interval`
   - 关闭副本（导入完成后再开启）

## 常见问题

### Q: 连接 Elasticsearch 失败？
A: 检查以下几点：
- ES 服务是否运行
- ES 地址是否正确
- 网络连接是否正常
- 用户名密码是否正确（如有）
- 防火墙是否放行

### Q: 导入速度慢？
A: 尝试：
- 使用 Release 版本
- 增大批次大小
- 检查网络带宽
- 优化 ES 配置

### Q: 部分记录导入失败？
A: 查看日志区的错误信息，常见原因：
- 索引 mapping 不匹配
- 字段值过长
- ES 磁盘空间不足

### Q: 如何处理 GBK 编码的 CSV？
A: 无需特殊处理，程序会自动检测并转换

## 日志说明

日志显示颜色：
- **白色**：普通信息
- **黄色**：警告（如跳过某些记录）
- **红色**：错误（如网络错误、ES 错误）

## 高级用法

### 大文件处理

对于超大 CSV 文件（GB 级别）：
1. 程序会自动估算行数
2. 如超过 ES 单索引上限，自动分块
3. 实时显示进度和已导入行数

### 批量导入多个文件

1. 使用"选择文件"或"选择文件夹"
2. 程序会依次处理每个文件
3. 每个文件的统计信息会分别显示

## 技术架构

- **前端**: eframe/egui (原生 GUI)
- **CSV 解析**: csv crate + 自定义编码处理
- **HTTP**: reqwest (blocking 模式)
- **并发**: 后台线程 + crossbeam channel
- **编码**: encoding_rs (GBK/GB18030)

## 系统要求

- **操作系统**: Windows / Linux / macOS
- **Rust**: 1.70+
- **内存**: 建议 2GB+（大文件需要更多）
- **网络**: 能访问 Elasticsearch 服务

## 许可证

MIT License

---

**祝您使用愉快！如有问题，请提交 Issue。**
